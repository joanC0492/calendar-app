export const convertEventsToDateEvents = () => {
  // return events.map((event) => {
  //   event.start = parseISO(`${event.start}`);
  //   event.end = parseISO(`${event.end}`);
  //   // return event;
  //   return getEventsAdapter(event);
  // });
};